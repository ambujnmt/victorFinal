import React from 'react';

// This is an obsolete page but is kept as a valid component to prevent build errors.
// All functionality has been moved to MyPlanPage.tsx.
const ProfilePage: React.FC = () => {
  return null; 
};

export default ProfilePage;
