import React from 'react';

// This is an obsolete page but is kept as a valid component to prevent build errors.
// All functionality is handled within CommunityPage.tsx.
const PrayerPage: React.FC = () => {
  return null; 
};

export default PrayerPage;
